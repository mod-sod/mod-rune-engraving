#
# TEMPLATE — paste into conf/mod_rune_engraving.conf.dist, fill the placeholders. Not
# loaded from templates/ (only conf/*.conf.dist is). Read it in C++ with
#   sConfigMgr->GetOption<T>("RuneEngraving.<Name>", <default>);
# Ship every key with its documented default.
#

#
#    RuneEngraving.<Name>
#        Description: <What it controls, in one or two lines.>
#        Default:     <DEFAULT>
#

RuneEngraving.<Name> = <DEFAULT>
